import Typography from '@mui/material/Typography';
import styled from '@mui/material/styles/styled';

const InputExample = styled(Typography)({
  fontSize: '0.8rem',
});

export default InputExample;
